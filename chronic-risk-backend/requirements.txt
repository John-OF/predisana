# Dependencias del API en PRODUCCION (runtime). Solo directas: pip resuelve las transitivas.
# El pipeline de datos (SDV/CTGAN, que arrastra torch ~479 MB) va aparte en
# requirements-pipeline.txt: el servidor no lo necesita. Ver AUD-17 en BITACORA.
Flask==3.1.2
flask-cors==6.0.2
gunicorn==23.0.0
joblib==1.5.3
lightgbm==4.6.0
numpy==2.3.5
pandas==2.3.3
# Driver de Postgres para DATABASE_URL en el deploy (#7). Inocuo en dev con SQLite.
psycopg2-binary==2.9.10
scikit-learn==1.8.0
shap==0.50.0
SQLAlchemy==2.0.49
